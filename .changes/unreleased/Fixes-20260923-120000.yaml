kind: Fixes
body: Allow integer `attribute` in the `sort`, `join`, `unique`, and `groupby` Jinja filters (e.g. `sort(attribute=0)`), matching Jinja2 behavior
time: 2026-09-23T12:00:00.000000+00:00
custom:
    author: jeremyyeo
    issue: "16418"
    project: dbt
